# Monthly GitHub Actions version update: bumps SHA-pinned `uses:` lines to
# their latest releases and opens a PR on the deps/actions-update branch with
# a configurable hold period before merge. Delegates to the arcuru/actions
# reusable workflow; CI verifies via the normal PR checks.
name: "Deps: Actions Update"

on:
  schedule:
    - cron: "0 6 1 * *"
  workflow_dispatch:
    inputs:
      hold_days:
        description: "Days to hold before allowing merge"
        required: false
        default: "7"
        type: string

permissions:
  contents: write
  pull-requests: write

jobs:
  actions-update:
    uses: arcuru/actions/.github/workflows/actions-update.yml@31c804a9c15602300e5e292e80cac1ad5512a21a # v0.2.0
    with:
      hold-days: ${{ inputs.hold_days || '7' }}
      runs-on: ubicloud-standard-2
      environment: automation
      enable-magic-cache: "false"
      extra-substituters: https://cache.eidetica.dev
      extra-trusted-public-keys: cache.eidetica.dev-1:eND5gRJlbnool3ZLCWT2H8kkygWS8JcsU76HYXbWPBI=
    secrets: inherit
